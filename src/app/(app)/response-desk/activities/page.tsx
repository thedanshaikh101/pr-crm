import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="Activities" step={8} includes={["Calls, emails, meetings, tasks with due dates"]} />; }
