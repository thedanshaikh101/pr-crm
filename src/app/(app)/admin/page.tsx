import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="Super-admin" step={9} includes={["All accounts, MRR, usage, bounce and complaint rates, impersonate, feature flags, system status"]} />; }
