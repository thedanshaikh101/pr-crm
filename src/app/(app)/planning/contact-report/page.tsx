import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="Contact Report" step={7} includes={["Everything sent and every response for a contact or list"]} />; }
