import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="Calendar" step={7} includes={["Month and week views, colour by client, drag to reschedule"]} />; }
