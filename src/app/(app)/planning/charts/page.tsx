import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="Charts" step={7} includes={["Sends, open rate, coverage by type/sentiment/client, top journalists, list health"]} />; }
