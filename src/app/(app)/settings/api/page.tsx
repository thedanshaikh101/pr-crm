import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="API keys and Webhooks" step={6} includes={["Create and revoke keys; register webhook endpoints"]} />; }
