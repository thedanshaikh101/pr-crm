import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="Billing" step={6} includes={["Stripe checkout and portal, usage meters, invoices, cancel"]} />; }
