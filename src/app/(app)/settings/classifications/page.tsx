import { ModulePlanned } from "@/components/ModulePlanned";
export default function Page() { return <ModulePlanned name="Contact Classifications" step={6} includes={["Extend the classification list"]} />; }
